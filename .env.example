# This portfolio does not require environment variables to run.
#
# Keep this file as a safe reference for future public configuration.
# Never commit real secrets in `.env` or `.env.local`.
#
# Example:
# VITE_PUBLIC_SITE_URL="https://your-domain.com"
